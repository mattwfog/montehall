# ViTPose++ top-down 2D pose worker. Locally: uv venv .venvs/pose + this file (CPU/MPS torch from PyPI).
# On Spark the NVIDIA base image's torch/torchvision are constrained and not replaced.
transformers==5.1.0
huggingface_hub>=0.30,<2
safetensors>=0.4,<1
numpy>=1.26,<3
opencv-python-headless>=4.10,<5
Pillow>=10,<13
scipy>=1.14,<2  # transformers VitPose processor imports scipy.linalg.inv lazily
